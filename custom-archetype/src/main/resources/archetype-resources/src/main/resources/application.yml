server:
 port: ${port}

spring:
  application:
    name: ${artifactId}

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
    beans:
      enabled: true